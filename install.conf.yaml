- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.gitignore: gitignore_global
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.gvimrc: gvimrc
    ~/.bash_profile: bash_profile

- shell:
    - git submodule update
